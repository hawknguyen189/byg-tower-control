{
  "name": "byg-tower-control",
  "version": "1.0.0",
  "description": "NodeJS, ExpressJS, MongoDB, Redux ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client-side && npx yarn react-app:start",
    "server": "cd server-side && npm start",
    "start": "concurrently --kill-others \"npm run server\" \"npm run client\""
  },
  "author": "hawk",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}